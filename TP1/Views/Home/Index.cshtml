@using TP1.Models.ViewModels;
@model ViewModel
@{
    ViewData["Title"] = "Home Page";
}
@*
<h1>Today</h1>
@foreach (var item in Model)
{
    <h2>@Html.ActionLink(item.Name,"Details",)</h2>
}*@

<div class="text-center">
    <h1 class="display-4">Welcome yo</h1>
</div>
   <h2>Movie</h2>
   <li>@Model.Movie.Name</li>
   <h2>Customers</h2>
   <ul>
       @foreach (var customer in Model.Customers)
        {
            <li>@customer.Name</li>
        }
    </ul>

